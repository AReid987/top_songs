# Specifications for the CLI Assessment

Specs:
- [x] Have a CLI for interfacing with the application #CLI accepts user input do determine how many songs to scrape, and then lists those song name. Next it asks if user would like more info on a particular song and also provides the option to relist songs again. Finally, a user can exit and then, optionally choose to list a different number of songs.
- [x] Pull data from an external source #data is scraped from the billboard top 100 list using Nokogiri and open-uri
- [x] Implement both list and detail views #the number of songs chose is first listed and further details can be displayed based on user input. 
